; 小米动态照片帧提取工具配置文件
; 命令行参数会覆盖此处的设置

[Paths]
; 输入目录或文件路径
input_dir = ./photos

; 输出目录路径
output_dir = ./extracted_frames

[Extraction]
; 帧提取间隔，1表示提取所有帧
interval = 1

; 最大提取帧数限制
max_frames = 500

[Processing]
; 是否递归处理子目录 (true/false)
recursive = false
